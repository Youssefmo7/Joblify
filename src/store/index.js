import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    // Global state
  },
  mutations: {
    // Mutations
  },
  actions: {
    // Actions
  },
  modules: {
    // Modules
  }
})